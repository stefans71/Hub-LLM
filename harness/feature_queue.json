{
  "meta": {
    "version": "2.2",
    "phase": "Terminal Integration Fixes",
    "last_updated": "2024-01-24"
  },
  "queue": [
    {
      "id": "FIX-01",
      "name": "Debug WebSocket Connection",
      "size": "M",
      "priority": 1,
      "status": "pending",
      "description": "Terminal WebSocket shows 'connection failed'. Debug and fix.",
      "symptoms": [
        "Terminal shows 'Connecting...' then 'WebSocket connection failed'",
        "IP shown is 192.168.1.104 (wrong - should be Digital Ocean IP)",
        "Project shows 'Connected' but terminal doesn't connect"
      ],
      "debug_steps": [
        "1. Check browser console for WebSocket errors",
        "2. Check backend logs when WebSocket connects",
        "3. Verify /api/terminal/ws endpoint is registered",
        "4. Check if project has correct vps_server_id in database",
        "5. Verify VPS credentials are being retrieved correctly"
      ],
      "test": "Terminal connects and shows real bash prompt from VPS"
    },
    {
      "id": "FIX-02",
      "name": "Fix Project-VPS Linking",
      "size": "S",
      "priority": 2,
      "status": "pending",
      "description": "Project shows wrong IP (192.168.1.104 instead of DO server). Fix project-to-VPS association.",
      "symptoms": [
        "Top bar shows 'prod-01 (192.168.1.104)' but user's DO server is different",
        "This is mock/hardcoded data, not from database"
      ],
      "check": [
        "WorkspaceTopBar.jsx - where does it get server info?",
        "LLMDevPanel.jsx - where does it get server info?",
        "API endpoint that returns project details with VPS info"
      ],
      "test": "Workspace shows correct DO server IP from Settings > VPS Connections"
    },
    {
      "id": "FIX-03",
      "name": "Wire FileBrowser to Real API",
      "size": "S",
      "priority": 3,
      "status": "pending",
      "description": "File explorer still shows mock data (server.js, routes.js). Connect to /api/files endpoint.",
      "symptoms": [
        "Shows hardcoded fake files, not real VPS directory",
        "Folders like 'src', 'index.js' are mock data"
      ],
      "check": [
        "FileBrowser.jsx - is it calling /api/files?",
        "Check network tab - does it make API request?",
        "Does API return data or error?"
      ],
      "test": "File browser shows real files from VPS home directory"
    },
    {
      "id": "FIX-04",
      "name": "Test All LLM-Dev Panel Functions",
      "size": "S",
      "priority": 4,
      "status": "pending",
      "description": "Verify all panel functionality works with real data",
      "checklist": [
        "Terminal tab: Real bash prompt, can run commands",
        "File Explorer: Shows real files, can expand folders",
        "Code Editor: Click file → loads content, can edit/save",
        "Add file button: Creates file on VPS",
        "Add folder button: Creates folder on VPS", 
        "Refresh button: Reloads file listing",
        "Docker tab: Shows real containers (or 'no containers' if none)",
        "Logs tab: Shows real logs or empty state",
        "Project Context tab: Shows correct project info"
      ],
      "test": "All checklist items pass"
    }
  ],
  "notes": {
    "user_vps": "User has Digital Ocean VPS connected in Settings > VPS Connections showing 'Connected'",
    "project_created": "Project 'Fridge Meal' created with VPS connection during setup",
    "issue": "Integration written but not wired to real project/VPS data"
  }
}
